/**
 * Cor única da polyline no fluxo de viagem (branding Zamba verde).
 * Usar em currentRide, map (pré-corrida), ecrãs relacionados — sem azul na rota.
 */
export const ROUTE_POLYLINE_COLOR = '#10B981';
